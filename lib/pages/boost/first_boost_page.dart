import 'package:flutter/material.dart';

class FirstBoostPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('FirstBoostPage'),
        ),
    );
  }
}