import 'package:flutter/material.dart';

class SecondBoostPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('SecondBoostPage'),
        ),
    );
  }
}